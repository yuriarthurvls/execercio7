<h1>Lista de Usuários</h1>

<ul>
{{#each usuarios}}
    <li>
        {{nome}} - {{email}} - {{idade}}

        <a href="/usuarios/remover/{{id}}">
            <button>Remover</button>
        </a>
    </li>
{{/each}}
</ul>